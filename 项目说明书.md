# 溯澜录 项目说明书

## 1. 项目定位
“溯澜录”面向水生生物实验室与野外采样场景，提供从图片采集、物种检索到报告导出的完整闭环。强调本地优先存储与可自定义路径，确保科研照片不混入系统相册。

## 2. 核心目标
- **本地优先**：所有图片先落地到 App 私有路径或自定义路径，优先保证本地可用。
- **路径可控**：支持自定义存储路径并自动 `.nomedia` 隔离，系统相册不可见。
- **统一 UI**：全局遵循首页风格与配色，保持一致的视觉语言。
- **高效导出**：一键导出图片 ZIP 与鉴定报告 Word，支持导出质量调节。
- **完整物种库**：内置物种名录，支持四大类与中文/拉丁名检索。

## 3. 技术架构
- **开发语言**：Kotlin
- **UI 框架**：Jetpack Compose
- **本地数据**：JSON 状态存储（LocalAppStore） + 文件系统
- **大数据镜像**：图片达到约 1000 张后双写 SQLite 元数据镜像；LocalAppStore 接口保持不变，后续可平滑迁移 Room
- **物种库**：内置 JSON 资源 + Excel 导入
- **图片处理**：Coil（预览）+ CameraX Extensions（HDR/夜景/自动增强）
- **文档导出**：Apache POI（Word/Excel 报告）+ Android PdfDocument（PDF）+ CSV
- **数据可靠性**：schema 迁移、原子写入、可恢复备份、带校验的 ZIP 备份与导入回滚
- **云端扩展**：Supabase 可选配置、统一字段协议和 RLS 策略迁移；未配置时保持完全本地优先

## 4. 功能模块
- **首页**：入口引导与最近数据集。
- **数据集管理**：创建、二次编辑、采样地点/坐标/水文参数/样品编号元数据、采样事件轨迹、二维码和样品链追踪。
- **相机连拍**：CameraX 连续拍照，本地保存并可立即关联物种，支持画质增强模式。
- **图库**：本地导入、稳定分页预加载、列表/网格预览、统一多字段检索、收藏、置信度与复核记录、批量关联/重命名/复核、全屏浏览、标注叠加、缩放/滑动查看、勾选导出与文件夹直出。
- **鉴定工作台**：待复核队列、候选物种对比、阈值筛选和鉴定历史。
- **分类库**：搜索、导入/编辑、异名/分类版本/文献/分布维护与可展开思维导图浏览。
- **数据集备份**：本地备份、系统分享与导入恢复。
- **报告中心**：模板、统计摘要和 CSV/Excel/Word/PDF 导出。
- **设置**：存储路径、相册保存开关、导出质量、相机画质增强、主题/动效、操作历史、备份提醒、隐私统计、GitHub 更新下载与加密文档查看。

## 5. 存储策略
- **App 私有路径**：默认路径，系统相册不可见。
- **自定义路径**：用户选择目录，自动创建子目录与 `.nomedia`，保证相册隔离。
- **导出**：支持用户选择保存路径（ZIP/Word）。
- **备份恢复**：导入前展示数据集、图片数量和大小；同名默认重命名，单项失败可追踪，取消或异常时回滚已复制图片。
- **项目文档**：说明书和开发进展书以 AES-256-GCM 资源保存，密码验证不持久化，后台/冷启动后重新验证。

## 6. 迭代规范
- **必做流程**：每次迭代必须同步更新更新日志、项目说明书、开发进展书、版本号和 TODO，并推送 GitHub。
- **历史归档**：每次打包需保留历史 APK 存档。

## 7. 项目信息
- **应用包名**：`com.dlovel`
- **应用名称**：溯澜录
- **版本号**：v1.7.8（versionCode 25）
- **开发单位**：江西水利电力大学
- **开发人员**：邓梓杰
